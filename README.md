# CoffeeGo - Мобільний застосунок для замовлення кави

Це React Native застосунок, створений з використанням Expo Router для навігації. Застосунок демонструє повноцінну інтеграцію навігації з використанням Stack, Tab та Drawer навігації.

## Структура навігації

### Stack Navigation (Лінійні переходи)
- **Головний екран → Деталі товару** (`/product/[id]`)
  - Передача параметра `id` через навігацію
  - Валідація параметрів та обробка помилок
  
- **Історія замовлень → Деталі замовлення** (`/order/[id]`)
  - Передача параметра `id` для відображення деталей замовлення

- **Профіль → Налаштування/Підтримка/Про застосунок**
  - Переходи до додаткових екранів з профілю

### Tab Navigation (Основні розділи)
- **Головна** (`/home`) - Список доступних товарів (кави)
- **Кошик** (`/cart`) - Товари, додані до кошика
- **Історія** (`/orders`) - Список попередніх замовлень
- **Профіль** (`/profile`) - Інформація про користувача та налаштування

### Drawer Navigation (Додаткові функції)
Доступні через профіль:
- **Налаштування** (`/settings`) - Налаштування застосунку
- **Підтримка** (`/support`) - Контактна інформація та FAQ
- **Про застосунок** (`/about`) - Інформація про застосунок

## 🏗️ Архітектура

### Структура файлів

```
app/
├── _layout.tsx              # Головний Stack навігатор
├── (tabs)/
│   ├── _layout.tsx          # Tab навігатор
│   ├── index.tsx            # Екран авторизації
│   ├── onboarding.tsx       # Екран онбордингу
│   ├── home.tsx             # Головний екран зі списком товарів
│   ├── cart.tsx             # Екран кошика
│   ├── orders.tsx           # Екран історії замовлень
│   └── profile.tsx          # Екран профілю
├── product/
│   └── [id].tsx             # Екран деталей товару (динамічний роут)
├── order/
│   └── [id].tsx             # Екран деталей замовлення (динамічний роут)
├── settings.tsx             # Екран налаштувань
├── support.tsx             # Екран підтримки
├── about.tsx                # Екран про застосунок
└── modal.tsx                # Модальний екран

components/
└── coffee/
    ├── ProductCard.tsx      # Компонент картки товару
    ├── PrimaryButton.tsx    # Основна кнопка
    ├── OutlineButton.tsx    # Кнопка з обводкою
    ├── FeatureCard.tsx      # Картка функції
    └── ...                  # Інші компоненти

constants/
├── screens.ts               # Константи назв екранів
└── coffeeTheme.ts           # Тема дизайну застосунку
```

## Передача даних між екранами

### Приклад передачі параметрів

```typescript
// Головний екран - передача параметра
const router = useRouter();
router.push({
  pathname: '/product/[id]',
  params: { id: productId },
});

// Екран деталей - отримання параметра
const { id } = useLocalSearchParams<{ id: string }>();
```

### Валідація параметрів

Всі екрани з динамічними параметрами включають валідацію:
- Перевірка наявності параметра
- Обробка випадку, коли дані не знайдено
- Відображення повідомлень про помилки

## Стилізація навігації

### Stack Navigation
- Кастомна тема, що відповідає дизайну застосунку
- Стилізовані заголовки з кольорами з теми
- Кнопки "Назад" з правильними кольорами

### Tab Navigation
- Іконки для кожного розділу
- Активний/неактивний стан вкладок
- Стилізація відповідно до теми застосунку

## Компоненти

### ProductCard
Компонент для відображення товару в списку:
- Назва та опис товару
- Ціна
- Обробка натискання для переходу до деталей

### Навігаційні компоненти
- Використання кастомних кнопок (PrimaryButton, OutlineButton)
- Інтеграція з існуючими компонентами дизайну

## 🚀 Запуск застосунку

1. Встановіть залежності:
```bash
npm install
```

2. Запустіть застосунок:
```bash
npx expo start
```

3. Виберіть платформу:
- iOS симулятор
- Android емулятор
- Expo Go на фізичному пристрої

## Виконані вимоги

- Stack Navigation для лінійних переходів
- Tab Navigation для основних розділів
- Передача даних між екранами з валідацією
- Стилізація навігаційних елементів
- Модульна архітектура (окремі файли для навігації)
- Документація в коді (коментарі)
- Константи для назв екранів (SCREENS)
- Адаптивність для iOS та Android

## Примітки

- Використовується Expo Router для файлової навігації
- Всі дані поки що мокові (для демонстрації)
- Застосунок готовий до інтеграції з реальним API
- Всі екрани мають обробку помилок та валідацію

## Демонстрація навігації

### Основні сценарії:

1. **Головний екран → Деталі товару**
   - Натисніть на будь-який товар у списку
   - Перехід до екрану деталей з передачею ID
   - Кнопка "Назад" для повернення

2. **Tab навігація**
   - Перемикання між розділами через нижню панель
   - Головна, Кошик, Історія, Профіль

3. **Профіль → Додаткові екрани**
   - Натисніть на пункт меню в профілі
   - Перехід до Налаштувань, Підтримки або Про застосунок
   - Кнопка "Назад" у заголовку

4. **Історія → Деталі замовлення**
   - Натисніть на замовлення в історії
   - Перехід до деталей з передачею ID замовлення

---

Розроблено з використанням React Native, Expo Router, та принципів SOLID, KISS, DRY.
